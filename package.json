{
  "name": "server",
  "version": "1.0.0",
  "description": "WebSocket server for game synchronization",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["websocket", "game", "server"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
